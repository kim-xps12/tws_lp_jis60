(version 1)

(rule "Global copper to edge"
  (constraint edge_clearance (min 0.5mm))
)

(rule "Castellated MCU module edge exception"
  (condition "A.hasComponentClass('CastellatedModule')")
  (constraint edge_clearance (min -0.05mm))
)
